apiVersion: external-secrets.io/v1beta1
kind: SecretStore
metadata:
  name: projjeffsecretsstore
spec:
  provider:
    aws:
      service: SecretsManager
      region: us-east-2
      auth:
        jwt:
          serviceAccountRef:
            name: projjeffsa